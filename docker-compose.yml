services:
  hme:
    image: ghcr.io/spacex-3/hidemyemail-generator:latest
    container_name: hme
    ports:
      - "8080:8080"
    environment:
      - DATA_DIR=/app/data
    volumes:
      - ./data:/app/data
    restart: unless-stopped
